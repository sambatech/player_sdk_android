apply plugin: 'com.android.library'

android {
    compileSdkVersion 23
    buildToolsVersion "23.0.3"

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 23
        versionCode 11
        versionName "v0.2.4-beta"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    // DEV
    //compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(':googlemediaframework')

    // PROD
    //compile 'com.sambatech.player:gmf:0.2.0-2'
    compile 'com.sambatech.player.3rd:ima-android-sdk:3.1.3'

    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:23.3.0'
    compile 'com.google.android.gms:play-services-ads:8.4.0'
    compile 'org.bitbucket.b_c:jose4j:0.4.4'
}

//PUBLISHING

/*GROUP="com.sambatech.player"
POM_ARTIFACT_ID="gmf"
VERSION_NAME="0.2.0"

apply from: "${rootDir}/utils/publish.gradle"*/
